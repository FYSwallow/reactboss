import React from 'react';
import ReactDOM from 'react-dom';
import { Button } from 'antd-mobile';
import {HashRouter, Route} from 'react-router-dom';


ReactDOM.render((
    <Button type='primary'>xuexi</Button>
),document.getElementById('root'));